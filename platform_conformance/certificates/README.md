# Conformance-only TLS fixtures

These certificates and private keys are public deterministic test material.
They are valid only for loopback conformance checks, provide no secrecy, and
must never be used for deployment or identity provisioning.
